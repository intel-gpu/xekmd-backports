From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Pravalika Gurram <pravalika.gurram@intel.com>
Date: Tue, 7 Jul 2026 18:54:21 +0530
Subject: drm/xe: Handle DRM_BUDDY_CONTIGUOUS_ALLOCATION flag with fallback for older kernels

On older kernels CONTIGUOUS_ALLOCATION flag is not present, to handle its usage
provided a fallback: round up the allocation size to the next power
of two and trim the resulting blocks via drm_buddy_block_trim() to
satisfy contiguous allocation requests.

Reference:
	dc2fc00ba94de 
	drm/xe: Use DRM_BUDDY_CONTIGUOUS_ALLOCATION for contiguous allocations

Signed-off-by: Pravalika Gurram <pravalika.gurram@intel.com>
---
1 file changed, 19 insertions(+), 1 deletion(-)

diff --git a/drivers/gpu/drm/xe/xe_ttm_vram_mgr.c b/drivers/gpu/drm/xe/xe_ttm_vram_mgr.c
index 0000001..0000002 100644
--- a/drivers/gpu/drm/xe/xe_ttm_vram_mgr.c
+++ b/drivers/gpu/drm/xe/xe_ttm_vram_mgr.c
@@ -130,9 +130,10 @@
 
 	if (place->flags & TTM_PL_FLAG_TOPDOWN)
 		vres->flags |= GPU_BUDDY_TOPDOWN_ALLOCATION;
-
+#ifndef BPM_DRM_BUDDY_CONTIGUOUS_ALLOCATION_NOT_PRESENT
 	if (place->flags & TTM_PL_FLAG_CONTIGUOUS)
 		vres->flags |= GPU_BUDDY_CONTIGUOUS_ALLOCATION;
+#endif
 
 	if (place->fpfn || lpfn != man->size >> PAGE_SHIFT)
 		vres->flags |= GPU_BUDDY_RANGE_ALLOCATION;
@@ -163,7 +164,17 @@
 		goto error_unlock;
 	}
 
+#ifdef BPM_DRM_BUDDY_CONTIGUOUS_ALLOCATION_NOT_PRESENT
+	if (place->fpfn + (size >> PAGE_SHIFT) != lpfn &&
+	    place->flags & TTM_PL_FLAG_CONTIGUOUS) {
+		size = roundup_pow_of_two(size);
+		min_page_size = size;
+
+		lpfn = max_t(unsigned long, place->fpfn + (size >> PAGE_SHIFT), lpfn);
+	}
+#endif
+
 	err = xe_ttm_vram_buddy_alloc(mgr, (u64)place->fpfn << PAGE_SHIFT,
 				      (u64)lpfn << PAGE_SHIFT, size,
 				      min_page_size, &vres->blocks, vres->flags,
 				      tbo, &vres->used_visible_size);
@@ -170,3 +181,9 @@
 	if (err)
 		goto error_unlock;
+#ifdef BPM_DRM_BUDDY_CONTIGUOUS_ALLOCATION_NOT_PRESENT
+	if (place->flags & TTM_PL_FLAG_CONTIGUOUS) {
+		if (!gpu_buddy_block_trim(mm, NULL, vres->base.size, &vres->blocks))
+			size = vres->base.size;
+	}
+#endif
 	mutex_unlock(&mgr->lock);
-- 
2.43.0
