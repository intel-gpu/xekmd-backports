From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Pravalika Gurram <pravalika.gurram@intel.com>
Date: Mon, 13 Jul 2026 20:54:29 +0530
Subject: [PATCH] mm/migrate_device: Disable THP migration on kernels < 6.19

Disable THP migration when MIGRATE_VMA_SELECT_COMPOUND and
MIGRATE_PFN_COMPOUND flags are unavailable (kernel < 6.19).

THP pages fall back to splitting and 4KB page-by-page migration.

LIMITATIONS:
- Zone device memory fragmented into 4KB pages

Reference:
	a30b48bf1b2
	mm/migrate_device: implement THP migration of zone device pages

Signed-off-by: Pravalika Gurram <pravalika.gurram@intel.com>
---
 drivers/gpu/drm/drm_pagemap.c | 21 ++++++++++++++++++++-
 1 file changed, 20 insertions(+), 1 deletion(-)

diff --git a/drivers/gpu/drm/drm_pagemap.c b/drivers/gpu/drm/drm_pagemap.c
index b36e4f5..b5c9c29 100644
--- a/drivers/gpu/drm/drm_pagemap.c
+++ b/drivers/gpu/drm/drm_pagemap.c
@@ -464,6 +464,7 @@ out:
 	return ret;
 }
 
+#ifndef BPM_MIGRATE_VMA_SELECT_COMPOUND_NOT_PRESENT
 /**
  * drm_pagemap_cpages() - Count collected pages
  * @migrate_pfn: Array of migrate_pfn entries to account
@@ -498,6 +499,7 @@ static int drm_pagemap_cpages(unsigned long *migrate_pfn, unsigned long npages)
 
 	return cpages;
 }
+#endif
 
 /**
  * drm_pagemap_migrate_to_devmem() - Migrate a struct mm_struct range to device memory
@@ -535,8 +537,13 @@ int drm_pagemap_migrate_to_devmem(struct drm_pagemap_devmem *devmem_allocation,
 		.start		= start,
 		.end		= end,
 		.pgmap_owner	= pagemap->owner,
+#ifdef BPM_MIGRATE_VMA_SELECT_COMPOUND_NOT_PRESENT
+		.flags		= MIGRATE_VMA_SELECT_SYSTEM | MIGRATE_VMA_SELECT_DEVICE_COHERENT |
+		MIGRATE_VMA_SELECT_DEVICE_PRIVATE,
+#else
 		.flags		= MIGRATE_VMA_SELECT_SYSTEM | MIGRATE_VMA_SELECT_DEVICE_COHERENT |
 		MIGRATE_VMA_SELECT_DEVICE_PRIVATE | MIGRATE_VMA_SELECT_COMPOUND,
+#endif
 	};
 	unsigned long i, npages = npages_in_range(start, end);
 	unsigned long own_pages = 0, migrated_pages = 0;
@@ -601,8 +608,12 @@ int drm_pagemap_migrate_to_devmem(struct drm_pagemap_devmem *devmem_allocation,
 		goto err_free;
 	}
 
+#ifdef BPM_MIGRATE_VMA_SELECT_COMPOUND_NOT_PRESENT
+	if (migrate.cpages != npages) {
+#else
 	if (migrate.cpages != npages &&
 	    drm_pagemap_cpages(migrate.src, npages) != npages) {
+#endif
 		/*
 		 * Some pages to migrate. But we want to migrate all or
 		 * nothing. Raced or unknown device pages.
@@ -675,6 +686,7 @@ int drm_pagemap_migrate_to_devmem(struct drm_pagemap_devmem *devmem_allocation,
 		}
 		migrate.dst[i] = migrate_pfn(migrate.dst[i]);
 
+#ifndef BPM_MIGRATE_VMA_SELECT_COMPOUND_NOT_PRESENT
 		if (migrate.src[i] & MIGRATE_PFN_COMPOUND) {
 			drm_WARN_ONCE(dpagemap->drm, src_page &&
 				      folio_order(page_folio(src_page)) != HPAGE_PMD_ORDER,
@@ -686,7 +698,7 @@ int drm_pagemap_migrate_to_devmem(struct drm_pagemap_devmem *devmem_allocation,
 			for (j = 1; j < NR_PAGES(order) && i + j < npages; j++)
 				migrate.dst[i + j] = 0;
 		}
-
+#endif
 		drm_pagemap_get_devmem_page(page, order, zdd);
 
 		/* If we switched the migrating drm_pagemap, migrate previous pages now */
@@ -814,8 +826,10 @@ static int drm_pagemap_migrate_populate_ram_pfn(struct vm_area_struct *vas,
 		page = folio_page(folio, 0);
 		mpfn[i] = migrate_pfn(page_to_pfn(page));
 
+#ifndef BPM_MIGRATE_VMA_SELECT_COMPOUND_NOT_PRESENT
 		if (order)
 			mpfn[i] |= MIGRATE_PFN_COMPOUND;
+#endif
 next:
 		if (page)
 			addr += page_size(page);
@@ -1137,9 +1151,14 @@ static int __drm_pagemap_migrate_to_ram(struct vm_area_struct *vas,
 	struct migrate_vma migrate = {
 		.vma		= vas,
 		.pgmap_owner	= page_pgmap(page)->owner,
+#ifdef BPM_MIGRATE_VMA_SELECT_COMPOUND_NOT_PRESENT
+		.flags		= MIGRATE_VMA_SELECT_DEVICE_PRIVATE |
+				  MIGRATE_VMA_SELECT_DEVICE_COHERENT, 
+#else
 		.flags		= MIGRATE_VMA_SELECT_DEVICE_PRIVATE |
 				  MIGRATE_VMA_SELECT_DEVICE_COHERENT |
 				  MIGRATE_VMA_SELECT_COMPOUND,
+#endif
 		.fault_page	= page,
 	};
 	struct drm_pagemap_migrate_details mdetails = {};
-- 
2.43.0

